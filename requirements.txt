# Arabic Audio Transcriber Requirements
# Install with: pip install -r requirements.txt

# Core audio processing
soundcard>=0.4.2
sounddevice>=0.4.6
numpy>=1.21.0

# Speech recognition
SpeechRecognition>=3.10.0

# Machine learning and translation
transformers>=4.21.0
huggingface-hub>=0.34.0,<1.0
torch>=1.12.0
sentencepiece>=0.1.97
protobuf>=3.20.0
tqdm>=4.66.0

# Keyboard shortcuts
keyboard>=0.13.5

# Offline/local TTS fallback for Speak Mode (Piper executable + tts_models/ is preferred when available)
pyttsx3>=2.90

# Windows notification-area menu (macOS MenuBarExtra counterpart)
pystray>=0.19.5
Pillow>=10.0.0

# Windows packaging
pyinstaller>=6.0.0

# Configuration management (built-in)
# configparser - included in Python standard library

# Additional dependencies that may be needed
# pyaudio>=0.2.11  # Alternative audio backend (optional)
# tensorflow>=2.9.0  # Alternative ML backend (optional)
